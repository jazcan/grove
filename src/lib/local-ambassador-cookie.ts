/** Persists `?ref=` on `/signup` through navigation (HttpOnly; cleared after successful sign-up). */
export const REFERRAL_COOKIE_NAME = "hl_ref";

export const REFERRAL_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 30;
